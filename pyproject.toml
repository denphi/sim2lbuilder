[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "sim2lbuilder"
authors = [{name = "Daniel Mejiai", email = "dmejiapa@purdue.edu"}]
dynamic = ["version", "description"]